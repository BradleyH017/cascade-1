% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/categorize_features.R
\name{categorize_features}
\alias{categorize_features}
\title{Categorize features (genes or peaks)}
\usage{
categorize_features(
  feature_data,
  lfsr_results,
  susie_results,
  meta_data,
  variant_feature_specificity,
  feature_type = "gene",
  hierarchy = DEFAULT_CELL_HIERARCHY,
  lfsr_sig_threshold = LFSR_SIG_THRESHOLD,
  lfsr_null_threshold = LFSR_NULL_THRESHOLD,
  cochran_q_threshold = 5e-08,
  use_cs_clusters = TRUE,
  cs_clusters = NULL,
  cs_cluster_variants = NULL
)
}
\arguments{
\item{feature_data}{Feature data from load_feature_data(config, feature_type, chromosomes)}

\item{lfsr_results}{Pre-computed LFSR results from load_lfsr_results()}

\item{susie_results}{SuSiE results (optional)}

\item{meta_data}{Required. Pre-computed meta data with Cochran's Q values}

\item{variant_feature_specificity}{Required. Variant-feature specificity mapping from variant categorization}

\item{feature_type}{Either "gene" or "peak"}

\item{hierarchy}{A CellTypeHierarchy object; defaults to DEFAULT_CELL_HIERARCHY}

\item{lfsr_sig_threshold}{Significance threshold used for both ACAT q-value
significance and LFSR gray zone lower bound (by design, both use the same
threshold). This is NOT acat_fdr_threshold (which is used only in variant
data loading).}

\item{lfsr_null_threshold}{LFSR null hypothesis threshold}

\item{cochran_q_threshold}{P-value threshold for Cochran's Q heterogeneity test (default 5e-8)}

\item{use_cs_clusters}{If TRUE, use credible-set cluster assignments when available; otherwise rely on Cochran's Q only}

\item{cs_clusters}{Optional credible-set cluster assignments (loaded by load_cs_clusters())}

\item{cs_cluster_variants}{Optional cluster-level variant details (loaded by load_cs_cluster_variants())}
}
\value{
List with two elements: categories (data frame with categorization results) and variant_details (data frame with variant details)
}
\description{
High-level dispatch for categorizing genes or peaks across cell types,
powered by vectorized C++ kernels.
}
